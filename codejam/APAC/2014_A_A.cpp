#include <cstdio>
#include <iostream>
#include <unordered_map>
#include <string>
using namespace std;

int main() {
	int T, N;
	int lights[105][7];
	scanf("%d", &T);
	for (int i = 0; i < T; ++i) {
		scanf("%d", &N);
		for (int j = 0; j < N; ++j) {
			for (int k = 0; k < 7; ++k) {
				scanf("%d", &lights[j][k]);
			}
		}
		vector<
	}
	return 0;
}
